# test eloss calculation and correction
#
#
import eloss as el
import numpy as np
nevent = 10000

l = 15.
dens = 0.167
z = 1.
a = 2.
mpart = 938.5
epart = 3140.

type = 4

de = []  # random

de0 = el.eloss(l, dens, z, a, epart, mpart, type) )

dem = el.eloss_results.eloss_mean # mean
demp = el.eloss_results.eloss_mp  # mostprobable

for i in range(i):
    de.append( el.eloss(l, dens, z, a, epart, mpart, type) )
#
de = np.array(de)
de_res = hist( de, range = (0., 20), bins = 41)

    
